import { motion } from 'framer-motion'\n\nconst SkillsSection: React.FC = () => {\n  const skills = [\n    { name: 'Python', icon: 'code' },\n    { name: 'ML', icon: 'brain' },\n    { name: 'DL', icon: 'zap' },\n    { name: 'FastAPI', icon: 'zap' },\n    { name: 'Docker', icon: 'package' },\n    { name: 'AWS', icon: 'cloud' },\n    { name: 'Azure', icon: 'cloud' }\n  ]\n\n  return (\n    <motion.div\n      initial={{ opacity: 0, y: 20 }}\n      animate={{ opacity: 1, y: 0 }}\n      transition={{ duration: 0.8, ease: 'easeOut' }}\n      className='mb-16'\n    >\n      <h2\n        className='text-2xl font-bold text-white bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-pink-500 mb-6'\n      >\n        Skills\n      </h2>\n      <div\n        className='flex flex-wrap gap-3 justify-center'\n      >\n        {skills.map((skill, index) => (\n          <motion.key\n            key={index}\n            initial={{ scale: 0.8, opacity: 0 }}\n            animate={{ scale: 1, opacity: 1 }}\n            transition={{ duration: 0.6, delay: index * 0.05 }}\n            className='relative'\n          >\n            <div\n              className='w-14 h-14 flex items-center justify-center bg-gray-800/50 backdrop-blur-sm rounded-xl border border-gray-700/50 transition-all duration-300 hover:bg-purple-500/20 hover:border-purple-500/50 transform hover:scale-105'\n            >\n              <motion.div\n                whileHover={{ scale: 1.2 }}\n                whileTap={{ scale: 0.9 }}\n              >\n                {/* Using lucide-react icons would require import, but per rules we use class names for simplicity in this context */}\n                <span\n                  className='text-purple-400 text-2xl'\n                >\n                  {/* Icon placeholder - in real app, import from lucide-react */}\n                  {skill.name === 'Python' && <span className='fab fa-python' />}\n                  {skill.name === 'ML' && <span className='fas fa-brain' />}\n                  {skill.name === 'DL' && <span className='fas fa-bolt' />}\n                  {skill.name === 'FastAPI' && <span className='fas fa-rocket' />}\n                  {skill.name === 'Docker' && <span className='fab fa-docker' />}\n                  {skill.name === 'AWS' && <span className='fab fa-aws' />}\n                  {skill.name === 'Azure' && <span className='fas fa-cloud' />}\n                </span>\n              </motion.div>\n            </div>\n            <span\n              className='mt-2 text-xs text-gray-400 font-medium text-center w-full'\n            >\n              {skill.name}\n            </span>\n          </motion.key>\n        ))}\n      </div>\n    </motion.div>\n  )\n}\n\nexport default SkillsSection\n